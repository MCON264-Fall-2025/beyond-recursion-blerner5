//I observed that both recursion and iteration are ways to get through a LinkedKist. 
//Recursion wokrs on one node at a time while itteration works on all.
//The coding for iteration is simpler.
//Recursion can lead to a stack or heap overflow.